import { Card, Textarea } from '@heroui/react';

const Header = () => {
  return (
    <Card as="header" css={{ padding: '1rem 0' }}>
      <Textarea >Insta-Meets</Textarea>
    </Card>
  );
};

export default Header;
