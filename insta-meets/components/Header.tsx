import { Container, Text } from '@heroui/react';

const Header = () => {
  return (
    <Container as="header" css={{ padding: '1rem 0' }}>
      <Text h2>Insta-Meets</Text>
    </Container>
  );
};

export default Header;
